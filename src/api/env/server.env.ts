import type { SERVER_TYPES } from "./server.type";

export const SERVER_ENV: SERVER_TYPES = {
  SERVER_HOST_DEV: "http://localhost:8000/api",
  SERVER_HOST_PROD: "https://api.disenmix.com/api",
  END_POINTS: {
    MAIL: "/contact",
  },
};
